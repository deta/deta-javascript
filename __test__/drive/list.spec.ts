// import { Deta } from '../../src/index';

// const projectKey = process.env.PROJECT_KEY || '';
// const driveName = process.env.DRIVE_NAME || '';

// const drive = Deta(projectKey).Drive(driveName);

describe.only('Drive#list', () => {
  it('list files', async () => {
    // const data = await drive.list(1);
    // const v = await data.next();
    // const b = await data.next();
    // console.log(v, b);
    expect(true).toEqual(true);
  });
});
