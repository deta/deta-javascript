/**
 * isUndefinedOrNull returns true if the provided value is of type undefined or null
 *
 * @param {any} value
 * @returns {boolean}
 */
export function isUndefinedOrNull(value: any): boolean {
  return value === undefined || value === null;
}
